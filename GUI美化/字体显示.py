from ttkbootstrap import fonts

# See the available font families
print(dir(fonts.available_fonts))

# See the available font sizes
print(dir(fonts.available_sizes))

# See the available font weights
print(dir(fonts.available_weights))